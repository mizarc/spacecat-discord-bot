"""
This is the primary entry point, which just points to the cli module.

It just runs the cli main function. Go there for more.
"""

from spacecat.cli import main

if __name__ == "__main__":
    main()
